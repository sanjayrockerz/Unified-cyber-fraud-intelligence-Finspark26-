# SDK Operations Guide

The Android SDK pairs through the backend, obtains scoped credentials, registers device/session telemetry, batches events, retries safely, and uses the authenticated WebSocket subprotocol for realtime updates. Validate registration, heartbeat, offline queue recovery, duplicate request IDs, and logout on a physical-device test plan before pilot approval.
