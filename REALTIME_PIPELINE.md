# Real-Time Pipeline

APK → Fusion SDK → authenticated REST → session/device identity → normalization → threat engine → graph runtime → ML runtime or explicit `ModelUnavailable` → decision engine → SQLite/event broker → authenticated WebSocket → Operations Center and APK response.
