# Unified Cyber-Fraud Intelligence Platform — Python deps (ML + API)
# Python 3.11+ required. Install PyTorch/PyTorch Geometric separately if this platform's
# CUDA/CPU wheel isn't on PyPI under these exact pins — see comment below.

# --- data / ml core ---
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
imbalanced-learn==0.12.4
xgboost==2.1.3
lightgbm==4.5.0
shap==0.46.0

# --- graph ---
networkx==3.4.2
python-louvain==0.16
neo4j==5.26.0
torch
torch_geometric

# --- api ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==14.1
python-dotenv==1.0.1
pydantic==2.10.3
geoip2
# --- reporting ---
reportlab==4.2.5

# --- kaggle download ---
kaggle==1.6.17

# --- dev ---
pytest==8.3.4
httpx>=0.28.0
pytest-asyncio>=0.24.0

cryptography
