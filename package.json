{
  "name": "unified-cyber-fraud-intelligence-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "start": "npm run dev --prefix web"
  }
}
